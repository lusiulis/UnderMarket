import { StyleSheet } from "react-native"

export const CommonSyles = StyleSheet.create({
    baseShadow: {
        shadowColor: '#000',
        shadowOpacity: 0.25,
        shadowRadius: 3.5,
        elevation: 4,
    },
})

export const AppColors = {
    turquoise: "#46D9B5",
    darkOcean: "#1D5771",
    calidPink: "#E28888"
}